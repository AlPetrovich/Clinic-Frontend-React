import { types } from "../types/types";

const intilialState = {
    checking: true,
}   

export const authReducer = (state = intilialState, action) => {

    switch (action.type) {
         
        default:
            return state;
        }    
}